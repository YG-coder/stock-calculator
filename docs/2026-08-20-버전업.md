# 주식계산기.kr 버전업 기록 (2026-08-20)

커밋: `9a3bb80` — `chore: Next.js 16.3.1 등 의존성 최신 버전 업그레이드`
저장소: `~/Projects/Stock-calculator` (GitHub: YG-coder/stock-calculator)

## 의존성 업그레이드

| 패키지 | 전 | 후 |
|---|---|---|
| next | 16.2.2 | 16.3.1 |
| react / react-dom | 19.1.0 | 19.2.8 |
| eslint-config-next | 15.4.6 | 16.3.1 |
| @opennextjs/cloudflare | 1.11.0 | 1.20.2 |
| wrangler | 4.44.0 | 4.124.0 |
| tailwindcss / @tailwindcss/postcss | 4.1.15 | 4.3.3 |
| @types/node | ^20 | ^22 |

`eslint-config-next` 가 Next 15 대에 멈춰 있어 Next 16 규칙을 못 받고 있었음 — 이번에 정렬.

## 설정 현대화

- `eslint.config.mjs`: `FlatCompat` 제거 → `eslint-config-next/core-web-vitals`, `.../typescript` 를 flat config 로 직접 import. `@eslint/eslintrc` 의존성 제거.
- `next.config.ts`: CommonJS `module.exports` → ESM `export default` + `NextConfig` 타입.
- Next 16 에서 `next lint` 가 제거됨 → `lint`(eslint .), `typecheck`(tsc --noEmit), `check`(lint+typecheck+build) 스크립트 추가.
- package.json version 0.1.0 → 0.2.0.

## 코드 수정

- `compound-interest-calculator/page.tsx`: `'72의 법칙'` → `‘72의 법칙’` (react/no-unescaped-entities).
- `guides/[slug]/page.tsx`: 미사용 `RelatedCalculators` import 제거.

## 보류한 메이저 업그레이드 (검증 후 결정)

- **TypeScript 7.0.2**: `tsc --noEmit` 자체는 통과하지만 `typescript-eslint` 가 "does not support TS 7.0" 으로 실패 → 린트 전체가 죽음. typescript-eslint 대응 나오면 재시도.
- **ESLint 10.8.1**: `eslint-plugin-react` 가 `resolveBasedir` 에서 크래시 → 보류.

## 검증

lint / typecheck / build 모두 통과 (build 는 클라우드 컨테이너에서 Google Fonts 차단 때문에 `next/font/google` 임시 스텁 후 검증, 전체 라우트 정적 생성 확인).

## 남은 일

- 맥북에서 `npm install` 실행 필요 (로컬 node_modules 는 아직 구버전).
- `_to_delete/git-junk/` 폴더 삭제 (원격 세션 git 이 남긴 lock/tmp 파일).
