import{describe,expect,it}from"vitest";import{parseAnnualReturns,simulateSorr}from"@/lib/sorr";describe("SoRR",()=>{it("같은 수익률 집합도 인출 시 순서에 따라 결과가 달라진다",()=>{const r=[-.3,.2,.2,.2];const a=simulateSorr(1000,10,0,r),b=simulateSorr(1000,10,0,[...r].reverse());expect(a.terminal).not.toBeCloseTo(b.terminal,8)});it("인출이 없으면 순서와 무관하다",()=>{const r=[-.3,.2,.2,.2];expect(simulateSorr(1000,0,0,r).terminal).toBeCloseTo(simulateSorr(1000,0,0,[...r].reverse()).terminal,8)});it("문자열 수익률을 소수로 변환",()=>expect(parseAnnualReturns("-10, 5  20")).toEqual([-.1,.05,.2]))});
